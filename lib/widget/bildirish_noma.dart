import 'package:flutter/material.dart';

class BildirishNoma extends StatelessWidget {
  const BildirishNoma({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Saqlangan xabarlar"),
      ),
    );
  }
}
